const pages=[...document.querySelectorAll('.page')],dots=[...document.querySelectorAll('.dot')],musicBtn=document.getElementById('musicBtn');let current=1,musicStarted=false,audioCtx,master,gainTimer;function goTo(n){if(n<1||n>5)return;pages.forEach((p,i)=>p.classList.toggle('active',i===n-1));dots.forEach((d,i)=>d.classList.toggle('active',i===n-1));current=n;if(n===2)startTyping();if(n===5)startLetter()}function startMusic(){if(musicStarted)return;musicStarted=true;try{audioCtx=new(window.AudioContext||window.webkitAudioContext)();master=audioCtx.createGain();master.gain.value=.055;master.connect(audioCtx.destination);const notes=[261.63,329.63,392,329.63,293.66,349.23,440,349.23,329.63,392,523.25,493.88,440,392,329.63,293.66];let t=audioCtx.currentTime+.05;for(let r=0;r<3;r++){notes.forEach((f,i)=>{tone(f,t+i*.48,.38);});t+=notes.length*.48}musicStarted=true;updateMusicButton()}catch(e){}}function tone(freq,when,dur){const o=audioCtx.createOscillator(),g=audioCtx.createGain();o.type='sine';o.frequency.value=freq;g.gain.setValueAtTime(0,when);g.gain.linearRampToValueAtTime(.28,when+.025);g.gain.exponentialRampToValueAtTime(.001,when+dur);o.connect(g);g.connect(master);o.start(when);o.stop(when+dur+.03)}function toggleMusic(){if(!audioCtx){startMusic();return}if(audioCtx.state==='running'){audioCtx.suspend();updateMusicButton()}else{audioCtx.resume();updateMusicButton()}}function updateMusicButton(){if(musicBtn)musicBtn.textContent=!audioCtx||audioCtx.state==='running'?'♫ Music on':'♫ Music off'}let typed=false;function startTyping(){if(typed)return;typed=true;const el=document.getElementById('typeText'),text='Some people are memories. Some become the reason memories feel special.';let i=0;const timer=setInterval(()=>{el.textContent=text.slice(0,i++);if(i>text.length)clearInterval(timer)},34)}let letterStarted=false;function startLetter(){if(letterStarted)return;letterStarted=true;const card=document.getElementById('letter');card.style.opacity='0';card.style.transform='translateY(8px)';setTimeout(()=>{card.style.transition='opacity .9s ease,transform .9s ease';card.style.opacity='1';card.style.transform='none'},150)}function makeHeart(){const h=document.createElement('div');h.className='heart';h.textContent=['♡','♥','✦','·'][Math.floor(Math.random()*4)];h.style.left=Math.random()*100+'%';h.style.fontSize=10+Math.random()*18+'px';h.style.animationDuration=6+Math.random()*7+'s';h.style.animationDelay=Math.random()*2+'s';document.getElementById('hearts').appendChild(h);setTimeout(()=>h.remove(),15000)}setInterval(makeHeart,900);const canvas=document.getElementById('confetti'),ctx=canvas.getContext('2d');let pieces=[];function resize(){canvas.width=innerWidth;canvas.height=innerHeight}addEventListener('resize',resize);resize();function celebrate(){startMusic();pieces=Array.from({length:180},()=>({x:innerWidth/2+(Math.random()-.5)*100,y:innerHeight*.48,vx:(Math.random()-.5)*12,vy:-Math.random()*12-4,size:4+Math.random()*7,rot:Math.random()*6.2,vr:(Math.random()-.5)*.25,life:1,shape:Math.random()>.5?'rect':'circle'}));requestAnimationFrame(drawConfetti)}function drawConfetti(){ctx.clearRect(0,0,canvas.width,canvas.height);pieces.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.vy+=.18;p.rot+=p.vr;p.life-=.006;ctx.save();ctx.globalAlpha=Math.max(0,p.life);ctx.translate(p.x,p.y);ctx.rotate(p.rot);ctx.fillStyle=['#ff9fca','#ffd166','#b8a1ff','#ffffff','#7fe7d8'][Math.floor(Math.random()*5)];if(p.shape==='rect')ctx.fillRect(-p.size/2,-p.size/3,p.size,p.size*.7);else{ctx.beginPath();ctx.arc(0,0,p.size/2,0,Math.PI*2);ctx.fill()}ctx.restore()});pieces=pieces.filter(p=>p.life>0&&p.y<canvas.height+30);if(pieces.length)requestAnimationFrame(drawConfetti);else ctx.clearRect(0,0,canvas.width,canvas.height)}addEventListener('keydown',e=>{if(e.key==='ArrowRight'||e.key===' '){e.preventDefault();if(current<5)goTo(current+1)}if(e.key==='ArrowLeft'){e.preventDefault();if(current>1)goTo(current-1)}});